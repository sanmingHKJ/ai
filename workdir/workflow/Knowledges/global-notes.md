# global important notes

- 每生成一个Lua文件，必须立即创建对应的JSON配置文件（标准格式）
- `ClassID` and `NodeId` are automatically generated by MiniWorld Studio based on `ClassType` and an internal node counter. You should NOT include them in your JSON files.

