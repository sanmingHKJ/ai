services:
  - type: web
    name: water-delivery
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    plan: free
    envVars:
      - key: ADMIN_PASS
        value: water2024
      - key: NODE_ENV
        value: production
